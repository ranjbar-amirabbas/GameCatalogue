name: gamecatalogue

services:
  frontend:
    build:
      context: ./frontend/game-catalogue
      dockerfile: Dockerfile
    container_name: gamecatalogue-frontend
    ports:
      - "4200:80"
    networks:
      - gamecatalogue

networks:
  gamecatalogue:
    name: gamecatalogue
